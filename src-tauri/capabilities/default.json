{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — least-privilege: the dialog/process actions the app calls (open a file picker; restart to apply an update) plus the window controls the custom titlebar drives (drag, minimize, maximize/restore, close) since native decorations are off.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
