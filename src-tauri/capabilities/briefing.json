{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "briefing-window",
  "description": "The always-on-top briefing panel. Deliberately NOT added to default.json's window list: that capability grants dialog, process-exit/restart and updater permissions, none of which a read-only briefing panel has any business holding. It needs two things its own app commands can't provide: the ability to be dragged by its header, since it is frameless like the main window, and the ability to LISTEN for events, so a briefing regenerated by the hourly scheduler or by the main window reaches it (`briefing://updated`). Listening only — neither emit nor emit-to is granted, so this window can receive the app's broadcasts but never originate one. Everything else about the window — creating, showing, hiding, positioning — is owned by the Rust side in tray.rs, and the panel's data comes from PM's own commands, which are not ACL-gated.",
  "windows": ["briefing"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
